// post
// 서브 섹션으로 파이프라인의 끝, state의 끝에서 작업의 끝난 후 실행할 작업을 정의한다.
// 조건 종류 : always, changed, failure, success, unstable 등 있다.
// always : 파이프라인의 모든 상태에서 실행
// changed : 현재 빌드 상태가 이전 빌드의 상태와 달라진 경우에만 실행
// failure : 파이프라인이 실패한 경우에만 실행
// success : 파이프라인이 성공한 경우에만 실행
// unstable : 파이프라인이 불안정한 경우에만 실행

pipeline {
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
            post {
                always {
                    echo '항상 실행'
                }
                changed {
                    echo '이전 빌드와 상태가 달라졌을 때 실행'
                }
                failure {
                    echo '빌드가 실패했을 때 실행'
                }
                success {
                    echo '빌드가 성공할 때 실행'
                }
                unstable {
                    echo '빌드가 불안전 할 때 실행'
                }
            }
        }
    }
}
